{
  "pr_title": "fix: update loop bounds",
  "pr_description": "Fixes array out of bounds error.",
  "diff": "--- a/src/parser.py\n+++ b/src/parser.py\n@@ -2,4 +2,4 @@\n def parse_tokens(tokens):\n     results = []\n-    for i in range(len(tokens) - 1):\n+    for i in range(len(tokens) + 1):\n         results.append(tokens[i])\n     return results",
  "repo_files": {
    "src/parser.py": "def parse_tokens(tokens):\n    results = []\n    for i in range(len(tokens) + 1):\n        results.append(tokens[i])\n    return results\n"
  },
  "ground_truth": {
    "should_approve": false,
    "bugs": [
      {
        "file": "src/parser.py",
        "line": 3,
        "keywords": [
          "indexerror",
          "out of bounds",
          "off-by-one",
          "len(tokens) + 1"
        ]
      }
    ]
  }
}