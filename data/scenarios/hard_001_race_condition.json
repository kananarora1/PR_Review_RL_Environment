{
  "pr_title": "feat: counter inc",
  "pr_description": "Thread-safe counter.",
  "diff": "--- a/src/cnt.py\n+++ b/src/cnt.py\n@@ -4,4 +4,5 @@\n     def inc(self):\n-        with self.lock:\n-            self.v += 1\n+        temp = self.v\n+        with self.lock:\n+            self.v = temp + 1",
  "repo_files": {
    "src/cnt.py": "import threading\nclass Cnt:\n    def __init__(self): self.v = 0; self.lock = threading.Lock()\n    def inc(self):\n        temp = self.v\n        with self.lock:\n            self.v = temp + 1\n"
  },
  "ground_truth": {
    "should_approve": false,
    "bugs": [
      {
        "file": "src/cnt.py",
        "line": 5,
        "keywords": [
          "race condition",
          "read before lock",
          "non-atomic",
          "temp = self.v"
        ]
      }
    ]
  }
}