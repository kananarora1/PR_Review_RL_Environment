{
  "pr_title": "feat: user search",
  "pr_description": "Search users by email.",
  "diff": "--- a/src/db.py\n+++ b/src/db.py\n@@ -1,3 +1,4 @@\n def get_user(cur, email):\n-    cur.execute(\"SELECT * FROM u WHERE email=?\", (email,))\n+    q = f\"SELECT * FROM u WHERE email='{email}'\"\n+    cur.execute(q)\n     return cur.fetchone()",
  "repo_files": {
    "src/db.py": "def get_user(cur, email):\n    q = f\"SELECT * FROM u WHERE email='{email}'\"\n    cur.execute(q)\n    return cur.fetchone()\n"
  },
  "ground_truth": {
    "should_approve": false,
    "bugs": [
      {
        "file": "src/db.py",
        "line": 2,
        "keywords": [
          "sql injection",
          "f-string",
          "string interpolation",
          "parameterized"
        ]
      }
    ]
  }
}